@layout("layouts/main")
@set("title", "Annonser")

@section("body")
    <div class="container">
        <div class="row justify-content-start">
            @each(ad in ads)
                <div class="col">
                    @!component("components/ad-card", { ad })
                </div>
            @end
        </div>
    </div>
@end