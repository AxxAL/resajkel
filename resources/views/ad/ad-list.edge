@layout("layouts/main")
@set("title", "Annonser")

@section("body")
    <div class="container">
        <h1>Alla annonser</h1>
        @if(ads.length > 0)
            <div class="row align-items-start">
                @each(ad in ads)
                    @!component("components/ad-card", { ad })
                @end
            </div>
        @else
            <p>Här var det öde :P</p>
        @endif
        </div>
    </div>
@end