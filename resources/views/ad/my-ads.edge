@layout("layouts/main")
@set("title", "Mina Annonser")

@section("body")
    <div class="container">
        <div class="row">
            @each(ad in ads)
                <div class="col">
                    @!component("components/my-ad-card", { ad })
                </div>
            @end
        </div>
    </div>
@end