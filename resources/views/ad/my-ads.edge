@layout("layouts/main")
@set("title", "Mina Annonser")

@section("body")
    <div class="container">
        <h1>Dina annonser</h1>
        <div class="row align-items-start">
            @if(ads.length > 0)
                @each(ad in ads)
                    @!component("components/my-ad-card", { ad })
                @end
            @else
                <p>Här var det öde :P</p>
                <p>Just nu har du inga annonser. Du kan däremot skapa en annons genom att <a href="/ad/create">klicka här</a>.</p>
            @endif
        </div>
    </div>
@end